extends: default

rules:
  line-length:
    max: 120
    level: warning

  document-start:
    present: true
    level: warning

  indentation:
    spaces: 2
    level: warning

  trailing-spaces: enable

  comments: disable
