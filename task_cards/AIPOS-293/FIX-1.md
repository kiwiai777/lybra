# FIX-1 卡:AIPOS-293 — Option C 支持结构文件上传/指定 + 人话错误(Owner 实测打回)

- **铁证**:Owner 实测截图 2026-08-01——向路径框填导出的 /tmp/lybra-structure.yaml
  → "错误: Unknown error"。两缺口:①仅支持目录路径(生成式预览),不支持结构文件
  直接导入(与 CLI import 不对称,跨机携带 yaml 的真场景没入口);②错误文案裸奔
  "Unknown error"。
- **修法**:
  a) Option C 双模式:模式一"指定项目目录"(现状);模式二"上传/指定结构文件"
     (file input 上传 或 服务器路径填 .yaml;二选一 UI 明确);
  b) 结构文件模式:上传/读取→schema 校验→预览(渲染解析出的项目名/仓库/映射)→
     确认导入(走既有 import 通道,幂等/非空保护/不 rm 红线不变);
  c) 错误全面人话化:后端错误映射 i18n 键(路径不存在/不是目录/不是合法结构文件/
     schema 校验失败列明字段),"Unknown error" 字样禁止出现(留兜底键但文案给出
     下一步建议);智能提示:目录框里填了 .yaml → 自动建议切模式二;
  d) 契约测试:双模式各一走通径+三类错误文案断言(i18n 双语);零回归。
- 车道:web/board/;写前重读。回报 RETURN-FIX-1.md。
