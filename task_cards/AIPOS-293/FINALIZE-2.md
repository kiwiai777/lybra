# FINALIZE-2 卡:293 收编补遗(顾问 pathspec 漏列 schema 文档)+草稿残留清理

- 授权链同 FINALIZE.md;性质=顾问漏列自纠(274FZ2 同款)。
- 范围:tools/aipos_cli/docs/project-structure-schema.md(=293 S1 文档,入 commit);
  tests/test_project_structure.py(根下,经 diff 判定=早期派次草稿,与 canonical 不同
  且未被引用→删除,删除动作与理由写入 RETURN);task_cards/AIPOS-293/ 本轮新档(-f)。
- 步骤:核对上两文件性质(schema 文档 head 应含 AIPOS-293 标头;草稿确认无引用)→
  commit+push→FINALIZE2-RETURN.md。
