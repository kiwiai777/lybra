# F-284B-1(P1):--expect 校验过严——"尚未存在的前缀目录"被布防拒绝,主用例反被堵死

- **发现**:2026-07-31 dogfood(277 哨兵)。--expect task_cards/AIPOS-277/RETURN.md
  在执行体尚未创建该目录时布防 → "prefix does not exist and cannot be created"拒绝,
  进程退出码 2(且与超时码撞车,语义混淆)。
- **定性**:F-284-1 修向要求拒绝的是**永不可能匹配**(越根/绝对/..)的模式;
  "静态前缀目前不存在"是合法的未来事件,watch 的本义就是等它出现。
  执行体把"不存在"实现成了"不可能"。dogfood 系列第二例(前:F-284-1)。
- **修向(FIX-1)**:a) 前缀不存在但在 workspace-root 内 → 合法,轮询中动态生效
  (每轮先 stat 前缀,存在后按 glob 扫);b) 拒绝仅限:绝对路径/..转义/解析后越出
  workspace-root;c) 布防拒绝时退出码独立(建议 5=USAGE,不与 2 超时混用)+stderr 单行
  原因;d) 测试:前缀后建场景(布防→sleep→mkdir+写文件→exit 0)+四出口回归。
- **处置**:284BR 审计在途,不混树;审结后 FIX-1 单独走环。当下 277 哨兵回退脚本版。
