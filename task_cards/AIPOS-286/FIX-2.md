# FIX-2 卡:AIPOS-286 — 生成类文案统一 i18n 通道(Owner 网页打回)

- **铁证**:verify_AIPOS-286_20260731T102521(reject:"提示词点英文后还是中文…双语能力
  能不能做一个统一接口不要每次都做一遍…所有说明提示词一定要可切全英文")。
- **病根**:接入提示词/向导说明在服务端硬编码中文拼串,不经 i18n;页面壳与生成内容
  双轨,每个新功能各翻一遍。
- **修法(统一接口)**:
  a) 建立唯一生成文案通道:服务端 locale 感知模板(zh/en 两套键值同源同键名,
     缺英文键=测试失败而非静默回退中文);前端语言切换把 locale 传给生成接口
     (查询参数或 header),返回对应语言全文;
  b) 覆盖面:接入提示词(顾问+牛马)、第 0 步、MCP 片段注释行、QUICKSTART 生成段、
     向导各说明块——全部走该通道;
  c) **硬断言**:en 模式下上述每段文案体不含 CJK 字符(路径/专名/卡号白名单除外),
     自动化测试逐段扫;zh 模式内容与现状等价;
  d) 后续新增文案必须走此通道:通道模块 docstring 写明规约,并加"游离文案侦测"测试
     (grep 生成函数外的硬编码中文串报警)可为宽松基线,列已知豁免清单。
- 车道:web/board/(app.py+static);铁律:写前重读盘上版本。回报 RETURN-FIX-2.md。
